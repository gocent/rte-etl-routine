# rte-etl-routine
Routine to poll RTE energy data

## Run project

The default run mode is `development`

### Prod
`RTE_ETL_ROUTINE_ENV=prod` go run main.go

### development
`RTE_ETL_ROUTINE_ENV=development` go run main.go

### Staging
`RTE_ETL_ROUTINE_ENV=staging` go run main.go